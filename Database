package Db;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;

public class Database {

    public static Connection getConnection() throws SQLException {
        String url = "jdbc:postgresql://ep-floral-frost-ac4zpawu-pooler.sa-east-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_gY8T2KBJEPQV&sslmode=require&channelBinding=require";
        try {
            Class.forName("org.postgresql.Driver");
        } catch (ClassNotFoundException e) {
            throw new SQLException("Driver PostgreSQL não encontrado!", e);
        }

        Connection conn = DriverManager.getConnection(url);
        System.out.println("Conectado ao banco: " + conn.getCatalog());
        return conn;
    }
}
